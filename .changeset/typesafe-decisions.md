---
"@eve/typesafe": patch
"eve": patch
---

Add `@eve/typesafe` with typed Jev decisions, an agent decision tool, and prompt-aware model selection that retains its choice across a turn or session. Dynamic model resolvers now receive the active cancellation signal so routing requests stop when their turn is cancelled.
