---
"eve": patch
---

Add the experimental `eve/experimental/typesafe` API with typed Jev decisions, an agent decision tool, and prompt-aware model selection from keyed model IDs or provider instances that retains its choice across a turn or session. Dynamic model resolvers now receive the active cancellation signal so routing requests stop when their turn is cancelled.
